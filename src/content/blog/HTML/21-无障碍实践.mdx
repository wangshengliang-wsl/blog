---
title: 无障碍实践
description: 学习 Web 无障碍开发的实践技巧，包括键盘导航、屏幕阅读器支持、焦点管理和常见无障碍问题的解决方案
pubDate: 2025-11-28
toc: true
ogImage: true
category: HTML
---

上一篇介绍了 ARIA 基础，这篇来看看实际开发中如何让网站对所有用户都友好，包括使用辅助技术的用户。

## 键盘导航

很多用户无法使用鼠标，只能通过键盘操作网页。确保键盘可访问是无障碍的基础。

### 基本键盘操作

| 按键        | 功能                   |
| ----------- | ---------------------- |
| Tab         | 移动到下一个可聚焦元素 |
| Shift + Tab | 移动到上一个可聚焦元素 |
| Enter       | 激活链接或按钮         |
| Space       | 激活按钮、选中复选框   |
| 方向键      | 在选项中移动           |
| Escape      | 关闭弹窗、取消操作     |

### 原生可聚焦元素

这些元素默认可以通过 Tab 聚焦：

```html
<a href="...">链接</a>
<button>按钮</button>
<input type="text" />
<select>
  ...
</select>
<textarea></textarea>
```

### 添加键盘支持

如果必须用 `<div>` 做交互元素，需要添加键盘支持：

```html
<div
  role="button"
  tabindex="0"
  onclick="handleClick()"
  onkeydown="handleKeyDown(event)"
>
  自定义按钮
</div>

<script>
  function handleKeyDown(event) {
    // 响应 Enter 和 Space
    if (event.key === 'Enter' || event.key === ' ') {
      event.preventDefault()
      handleClick()
    }
  }

  function handleClick() {
    console.log('按钮被激活')
  }
</script>
```

### tabindex 属性

```html
<!-- tabindex="0"：加入 Tab 序列 -->
<div tabindex="0">可聚焦的 div</div>

<!-- tabindex="-1"：可编程聚焦，但不在 Tab 序列中 -->
<div tabindex="-1" id="panel">面板内容</div>

<!-- tabindex > 0：指定顺序（不推荐） -->
<input tabindex="1" />
<input tabindex="2" />
```

`tabindex` 大于 0 会打乱自然 Tab 顺序，维护困难，应该避免使用。

### 跳过导航链接

让键盘用户可以跳过重复的导航：

```html
<body>
  <a href="#main-content" class="skip-link">跳到主内容</a>

  <header>
    <nav>
      <!-- 很多导航链接 -->
    </nav>
  </header>

  <main id="main-content" tabindex="-1">主要内容...</main>
</body>

<style>
  .skip-link {
    position: absolute;
    top: -40px;
    left: 0;
    padding: 8px 16px;
    background: #000;
    color: #fff;
    z-index: 100;
  }

  .skip-link:focus {
    top: 0;
  }
</style>
```

跳过链接平时隐藏，获得焦点时显示出来。

## 焦点管理

### 可见的焦点样式

永远不要完全移除焦点样式：

```css
/* 不好：完全移除焦点样式 */
:focus {
  outline: none;
}

/* 好：自定义焦点样式 */
:focus {
  outline: 2px solid #007bff;
  outline-offset: 2px;
}

/* 更好：只在键盘导航时显示焦点样式 */
:focus:not(:focus-visible) {
  outline: none;
}

:focus-visible {
  outline: 2px solid #007bff;
  outline-offset: 2px;
}
```

`:focus-visible` 只在键盘导航时显示焦点样式，鼠标点击时不显示。

### 焦点陷阱

模态对话框需要"陷阱"焦点，防止用户 Tab 到背后的内容：

```html
<div id="modal" role="dialog" aria-modal="true" aria-labelledby="modal-title">
  <h2 id="modal-title">对话框标题</h2>
  <p>对话框内容</p>
  <button id="close-btn">关闭</button>
</div>

<script>
  class FocusTrap {
    constructor(element) {
      this.element = element
      this.focusableElements = element.querySelectorAll(
        'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
      )
      this.firstElement = this.focusableElements[0]
      this.lastElement =
        this.focusableElements[this.focusableElements.length - 1]
    }

    activate() {
      this.element.addEventListener('keydown', this.handleKeyDown.bind(this))
      this.firstElement.focus()
    }

    deactivate() {
      this.element.removeEventListener('keydown', this.handleKeyDown.bind(this))
    }

    handleKeyDown(event) {
      if (event.key !== 'Tab') return

      if (event.shiftKey) {
        // Shift + Tab
        if (document.activeElement === this.firstElement) {
          event.preventDefault()
          this.lastElement.focus()
        }
      } else {
        // Tab
        if (document.activeElement === this.lastElement) {
          event.preventDefault()
          this.firstElement.focus()
        }
      }
    }
  }

  // 使用
  const modal = document.getElementById('modal')
  const focusTrap = new FocusTrap(modal)

  function openModal() {
    modal.hidden = false
    focusTrap.activate()
  }

  function closeModal() {
    modal.hidden = true
    focusTrap.deactivate()
  }
</script>
```

### 焦点恢复

关闭对话框后，应该把焦点返回到触发元素：

```javascript
let previouslyFocusedElement = null

function openModal() {
  // 保存当前焦点
  previouslyFocusedElement = document.activeElement

  modal.hidden = false
  focusTrap.activate()
}

function closeModal() {
  modal.hidden = true
  focusTrap.deactivate()

  // 恢复焦点
  if (previouslyFocusedElement) {
    previouslyFocusedElement.focus()
  }
}
```

### 动态内容焦点

当页面内容动态更新时，需要管理焦点：

```javascript
// 加载新内容后移动焦点
async function loadMoreContent() {
  const container = document.getElementById('content')
  const newContent = await fetchContent()

  // 添加内容
  container.insertAdjacentHTML('beforeend', newContent)

  // 聚焦到新内容
  const newItems = container.querySelectorAll('.new-item')
  if (newItems.length > 0) {
    newItems[0].focus()
  }
}

// 删除项目后移动焦点
function deleteItem(item) {
  const nextItem = item.nextElementSibling || item.previousElementSibling
  item.remove()

  if (nextItem) {
    nextItem.focus()
  }
}
```

## 屏幕阅读器支持

屏幕阅读器是视障用户访问网页的重要工具。

### 常见屏幕阅读器

| 平台    | 屏幕阅读器 | 特点           |
| ------- | ---------- | -------------- |
| Windows | NVDA       | 免费开源       |
| Windows | JAWS       | 功能强大，付费 |
| macOS   | VoiceOver  | 系统内置       |
| iOS     | VoiceOver  | 系统内置       |
| Android | TalkBack   | 系统内置       |

### 提供文本替代

图片必须有 alt 属性：

```html
<!-- 信息性图片：描述内容 -->
<img src="chart.png" alt="2024年销售数据柱状图，一季度最高" />

<!-- 功能性图片：描述功能 -->
<img src="search.svg" alt="搜索" />

<!-- 装饰性图片：空 alt -->
<img src="decoration.png" alt="" />

<!-- 复杂图表：详细描述 -->
<figure>
  <img src="complex-chart.png" alt="销售趋势图" aria-describedby="chart-desc" />
  <figcaption id="chart-desc">
    图表显示2024年各季度销售额：Q1为100万，Q2为120万，
    Q3为90万，Q4预计为150万。整体呈上升趋势。
  </figcaption>
</figure>
```

### 隐藏装饰性内容

对屏幕阅读器隐藏纯装饰内容：

```html
<!-- 装饰性图标 -->
<button>
  <span aria-hidden="true">🔔</span>
  通知
</button>

<!-- 装饰性分隔线 -->
<hr aria-hidden="true" />

<!-- 背景动画 -->
<div class="animation-bg" aria-hidden="true"></div>
```

### 只对屏幕阅读器可见

有时需要添加只有屏幕阅读器能"看到"的内容：

```html
<!-- 视觉隐藏但屏幕阅读器可读 -->
<span class="sr-only">当前页面：</span>首页

<style>
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
</style>
```

### 实时区域

动态更新的内容需要通知屏幕阅读器：

```html
<!-- 状态消息 -->
<div role="status" aria-live="polite">
  <!-- 动态内容 -->
</div>

<!-- 错误��息 -->
<div role="alert" aria-live="assertive">
  <!-- 紧急消息 -->
</div>

<script>
  // 更新状态
  function updateStatus(message) {
    const status = document.querySelector('[role="status"]')
    status.textContent = message
  }

  // 表单提交后
  function onSubmit() {
    updateStatus('正在保存...')
    // 保存完成后
    updateStatus('保存成功！')
  }
</script>
```

### 表单标签

每个表单控件都需要关联的标签：

```html
<!-- 方法1：label 包裹 -->
<label>
  用户名
  <input type="text" name="username" />
</label>

<!-- 方法2：for 关联 -->
<label for="email">邮箱</label>
<input type="email" id="email" name="email" />

<!-- 方法3：aria-label -->
<input type="search" aria-label="搜索文章" />

<!-- 方法4：aria-labelledby -->
<h2 id="billing">账单地址</h2>
<input type="text" aria-labelledby="billing" placeholder="街道地址" />
```

### 错误提示

表单错误需要正确关联：

```html
<div class="form-group">
  <label for="password">密码</label>
  <input
    type="password"
    id="password"
    aria-describedby="password-error password-hint"
    aria-invalid="true"
  />
  <p id="password-hint">至少8位，包含字母和数字</p>
  <p id="password-error" class="error" role="alert">密码格式不正确</p>
</div>
```

## 颜色和对比度

### 颜色对比度

文本和背景需要足够的对比度：

| 级别 | 正常文本 | 大文本（18px 以上） |
| ---- | -------- | ------------------- |
| AA   | 4.5:1    | 3:1                 |
| AAA  | 7:1      | 4.5:1               |

```css
/* 好：高对比度 */
.text {
  color: #333;
  background: #fff;
  /* 对比度约 12:1 */
}

/* 不好：低对比度 */
.text {
  color: #999;
  background: #fff;
  /* 对比度约 2.8:1 */
}
```

### 不仅依赖颜色

不要仅用颜色传达信息：

```html
<!-- 不好：只用颜色区分 -->
<span style="color: red;">必填</span>

<!-- 好：颜色 + 文字 -->
<span style="color: red;">* 必填</span>

<!-- 不好：只用颜色表示状态 -->
<span class="status-red"></span>

<!-- 好：颜色 + 图标 + 文字 -->
<span class="status-error">
  <svg aria-hidden="true">...</svg>
  错误
</span>
```

### 焦点样式

焦点指示器也需要足够的对比度：

```css
:focus-visible {
  /* 不好：浅色边框 */
  outline: 2px solid #ccc;

  /* 好：高对比度边框 */
  outline: 2px solid #005fcc;
  outline-offset: 2px;
}
```

## 响应式���障碍

### 触摸目标大小

触摸目标应该足够大：

```css
/* 按钮至少 44x44 像素 */
.button {
  min-width: 44px;
  min-height: 44px;
  padding: 12px 24px;
}

/* 链接也需要足够大的点击区域 */
.nav-link {
  display: inline-block;
  padding: 10px 16px;
}
```

### 缩放支持

不要禁止页面缩放：

```html
<!-- 不好：禁止缩放 -->
<meta
  name="viewport"
  content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no"
/>

<!-- 好：允许缩放 -->
<meta name="viewport" content="width=device-width, initial-scale=1" />
```

### 减少动画

尊重用户的动画偏好：

```css
/* 默认动画 */
.element {
  transition: transform 0.3s;
}

/* 用户偏好减少动画时 */
@media (prefers-reduced-motion: reduce) {
  .element {
    transition: none;
  }

  .animation {
    animation: none;
  }
}
```

```javascript
// JavaScript 中检测
const prefersReducedMotion = window.matchMedia(
  '(prefers-reduced-motion: reduce)'
).matches

if (prefersReducedMotion) {
  // 禁用动画或使用简单动画
}
```

## 测试无障碍

### 键盘测试

1. 把鼠标放到一边，只用键盘操作
2. 检查所有交互元素能否用 Tab 到达
3. 检查焦点顺序是否合理
4. 检查焦点样式是否可见
5. 检查 Enter/Space 能否激活按钮和链接

### 屏幕阅读器测试

```
VoiceOver (Mac):
- 打开: Cmd + F5
- 导航: Ctrl + Option + 方向键
- 激活: Ctrl + Option + Space

NVDA (Windows):
- 导航: Tab 或方向键
- 阅读: Insert + 方向键
- 停止: Ctrl
```

### 自动化测试

```javascript
// 使用 axe-core
import axe from 'axe-core'

axe.run(document, (err, results) => {
  if (err) throw err

  // 违规项
  console.log('Violations:', results.violations)

  // 通过项
  console.log('Passes:', results.passes)

  // 需要人工检查
  console.log('Incomplete:', results.incomplete)
})

// 与测试框架集成
describe('无障碍测试', () => {
  it('页面无违规', async () => {
    const results = await axe.run(document)
    expect(results.violations).toHaveLength(0)
  })
})
```

### 浏览器工具

Chrome DevTools 的 Lighthouse 和 Accessibility 面板可以检测常见问题：

1. 打开 DevTools (F12)
2. 选择 Lighthouse 标签
3. 勾选 Accessibility
4. 点击 Analyze page load

### 常见检查清单

```markdown
## 基础检查

- [ ] 所有图片有 alt 属性
- [ ] 页面有唯一的 h1
- [ ] 标题层级不跳级
- [ ] 表单控件有标签
- [ ] 链接文本有意义（不是"点击这里"）

## 键盘检查

- [ ] 所有交互元素可聚焦
- [ ] 焦点顺序合理
- [ ] 焦点样式可见
- [ ] 没有键盘陷阱（除了模态框）
- [ ] 有跳过导航链接

## 视觉检查

- [ ] 文本对比度足够
- [ ] 不仅依赖颜色传达信息
- [ ] 页面可以放大到 200%
- [ ] 支持减少动画偏好

## 屏幕阅读器检查

- [ ] 页面结构清晰
- [ ] 动态内容有通知
- [ ] 错误提示可访问
```

## 常见问题和解决方案

### 自定义下拉菜单

```html
<div class="custom-select">
  <button
    aria-haspopup="listbox"
    aria-expanded="false"
    aria-labelledby="select-label"
    id="select-button"
  >
    <span id="select-label">选择城市</span>
    <span id="selected-value">请选择</span>
  </button>

  <ul role="listbox" aria-labelledby="select-label" hidden>
    <li role="option" aria-selected="false">北京</li>
    <li role="option" aria-selected="false">上海</li>
    <li role="option" aria-selected="false">广州</li>
  </ul>
</div>

<script>
  const button = document.getElementById('select-button')
  const listbox = document.querySelector('[role="listbox"]')
  const options = document.querySelectorAll('[role="option"]')

  button.addEventListener('click', () => {
    const expanded = button.getAttribute('aria-expanded') === 'true'
    button.setAttribute('aria-expanded', !expanded)
    listbox.hidden = expanded
  })

  options.forEach((option) => {
    option.addEventListener('click', () => {
      // 更新选中状态
      options.forEach((opt) => opt.setAttribute('aria-selected', 'false'))
      option.setAttribute('aria-selected', 'true')

      // 更新显示值
      document.getElementById('selected-value').textContent = option.textContent

      // 关闭下拉
      button.setAttribute('aria-expanded', 'false')
      listbox.hidden = true
      button.focus()
    })
  })

  // 键盘导航
  listbox.addEventListener('keydown', (e) => {
    const currentOption =
      document.querySelector('[role="option"][aria-selected="true"]') ||
      options[0]

    switch (e.key) {
      case 'ArrowDown':
        e.preventDefault()
        const next = currentOption.nextElementSibling
        if (next) next.focus()
        break
      case 'ArrowUp':
        e.preventDefault()
        const prev = currentOption.previousElementSibling
        if (prev) prev.focus()
        break
      case 'Escape':
        button.setAttribute('aria-expanded', 'false')
        listbox.hidden = true
        button.focus()
        break
    }
  })
</script>
```

### 轮播图

```html
<div
  class="carousel"
  role="region"
  aria-roledescription="carousel"
  aria-label="产品展示"
>
  <button class="carousel-prev" aria-label="上一张">←</button>

  <div class="carousel-slides" aria-live="polite">
    <div role="group" aria-roledescription="slide" aria-label="1 / 3">
      <img src="product1.jpg" alt="产品1描述" />
    </div>
    <!-- 更多幻灯片 -->
  </div>

  <button class="carousel-next" aria-label="下一张">→</button>

  <div class="carousel-indicators" role="tablist">
    <button role="tab" aria-selected="true" aria-label="第1张">●</button>
    <button role="tab" aria-selected="false" aria-label="第2张">○</button>
    <button role="tab" aria-selected="false" aria-label="第3张">○</button>
  </div>
</div>
```

### 手风琴

```html
<div class="accordion">
  <h3>
    <button aria-expanded="true" aria-controls="panel1">
      第一节
      <span aria-hidden="true">▼</span>
    </button>
  </h3>
  <div id="panel1" role="region">第一节内容...</div>

  <h3>
    <button aria-expanded="false" aria-controls="panel2">
      第二节
      <span aria-hidden="true">▶</span>
    </button>
  </h3>
  <div id="panel2" role="region" hidden>第二节内容...</div>
</div>

<script>
  const buttons = document.querySelectorAll('.accordion button')

  buttons.forEach((button) => {
    button.addEventListener('click', () => {
      const expanded = button.getAttribute('aria-expanded') === 'true'
      const panel = document.getElementById(
        button.getAttribute('aria-controls')
      )

      button.setAttribute('aria-expanded', !expanded)
      panel.hidden = expanded
    })
  })
</script>
```

## 小结

无障碍开发是让网站对所有人可用的重要实践：

**键盘导航**：

- 使用原生可聚焦元素
- 为自定义组件添加键盘支持
- 提供跳过导航链接

**焦点管理**：

- 保持可见的焦点样式
- 模态框使用焦点陷阱
- 动态内容更新后管理焦点

**屏幕阅读器**：

- 提供文本替代
- 使用实时区域通知动态内容
- 正确关联表单标签和错误提示

**视觉设计**：

- 保证足够的颜色对比度
- 不仅依赖颜色传达信息
- 支持页面缩放和减少动画

**测试**：

- 键盘测试
- 屏幕阅读器测试
- 自动化工具（axe-core、Lighthouse）

下一篇会介绍语义化的最佳实践，把语义化和无障碍结合起来。
