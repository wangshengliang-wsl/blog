---
title: 常用属性
description: 全面了解 HTML 元素的常用全局属性，包括 id、class、style、data-* 自定义属性、hidden、tabindex 等
pubDate: 2025-11-28
toc: true
ogImage: true
category: HTML
---

HTML 元素除了各自的专有属性外，还有一些所有元素都可以使用的**全局属性**。这篇文章介绍这些常用的全局属性。

## id 属性

`id` 为元素指定唯一标识符：

```html
<h1 id="main-title">页面标题</h1>
<div id="content">内容区域</div>
```

### id 的命名规则

- 必须以字母开头
- 可以包含字母、数字、连字符（-）、下划线（\_）
- 区分大小写
- 在整个文档中必须唯一

```html
<!-- 有效的 id -->
<div id="main-content"></div>
<div id="section-1"></div>
<div id="user_profile"></div>

<!-- 无效的 id -->
<div id="123"></div>
<!-- 不能以数字开头 -->
<div id="my content"></div>
<!-- 不能有空格 -->
```

### id 的用途

#### 1. CSS 选择器

```css
#main-title {
  font-size: 2rem;
  color: #333;
}
```

id 选择器的优先级很高，一般推荐使用 class。

#### 2. JavaScript 获取元素

```javascript
const title = document.getElementById('main-title')
const content = document.querySelector('#content')
```

#### 3. 锚点链接

```html
<a href="#section1">跳转到第一节</a>

<h2 id="section1">第一节</h2>
```

#### 4. label 关联

```html
<label for="username">用户名</label> <input type="text" id="username" />
```

#### 5. ARIA 引用

```html
<button aria-describedby="help-text">帮助</button>
<p id="help-text">点击获取更多信息</p>
```

## class 属性

`class` 为元素指定一个或多个类名：

```html
<div class="container">
  <p class="text-large text-bold">大号粗体文字</p>
</div>
```

### class 的特点

- 多个类名用空格分隔
- 同一个类名可以用在多个元素上
- 不需要唯一

### class 的命名约定

常见的命名方法：

#### BEM（Block Element Modifier）

```html
<div class="card">
  <h2 class="card__title">标题</h2>
  <p class="card__content">内容</p>
  <button class="card__button card__button--primary">按钮</button>
</div>
```

- Block：独立的组件（card）
- Element：组件的一部分（card\_\_title）
- Modifier：变体或状态（card\_\_button--primary）

#### 功能性命名

```html
<div class="flex justify-between items-center">
  <span class="text-lg font-bold">标题</span>
  <button class="btn btn-primary">按钮</button>
</div>
```

这种方式在 Tailwind CSS 等工具类框架中很流行。

### class 与 id 的选择

| 场景                | 推荐  |
| ------------------- | ----- |
| 样式（CSS）         | class |
| JavaScript 唯一元素 | id    |
| 可复用的样式        | class |
| 锚点链接            | id    |
| 表单 label 关联     | id    |

## style 属性

`style` 用于设置内联样式：

```html
<p style="color: red; font-size: 18px;">红色文字</p>
```

### 内联样式的特点

- 优先级最高（仅次于 `!important`）
- 只作用于当前元素
- 不能使用伪类、伪元素、媒体查询

### 何时使用内联样式

一般情况下应该避免使用内联样式，但以下场景例外：

```html
<!-- 动态样式（由 JavaScript 计算�� -->
<div style="width: 75%;">进度条</div>

<!-- 邮件 HTML（很多邮件客户端不支持外部样式） -->
<td style="padding: 20px; background-color: #f5f5f5;">邮件内容</td>

<!-- 临时调试 -->
<div style="border: 1px solid red;">调试中</div>
```

## data-\* 自定义属性

`data-*` 属性用于存储自定义数据：

```html
<article data-id="123" data-category="technology" data-author-name="张三">
  文章内容
</article>
```

### 命名规则

- 必须以 `data-` 开头
- 不能包含大写字母
- 至少一个字符

### JavaScript 访问

```javascript
const article = document.querySelector('article')

// 通过 dataset 属性访问
console.log(article.dataset.id) // "123"
console.log(article.dataset.category) // "technology"
console.log(article.dataset.authorName) // "张三"（驼峰转换）

// 设置值
article.dataset.views = '1000'

// 通过 getAttribute
console.log(article.getAttribute('data-id'))
```

注意：`data-author-name` 在 JavaScript 中变成 `dataset.authorName`（连字符转驼峰）。

### CSS 访问

```css
/* 属性选择器 */
article[data-category='technology'] {
  border-left: 3px solid blue;
}

/* attr() 函数 */
article::before {
  content: attr(data-category);
}
```

### 常见用途

```html
<!-- 存储配置 -->
<div class="carousel" data-auto-play="true" data-interval="3000">
  <!-- 存储状态 -->
  <button data-loading="false">提交</button>

  <!-- 存储关联数据 -->
  <button data-user-id="123" data-action="delete">删除用户</button>

  <!-- 国际化 -->
  <span data-i18n="greeting">你好</span>
</div>
```

## hidden 属性

`hidden` 属性隐藏元素：

```html
<div hidden>这个元素不可见</div>
```

等同于 CSS 的 `display: none`，元素不会被渲染。

### hidden vs CSS 隐藏

```html
<!-- HTML hidden：语义上隐藏 -->
<div hidden>隐藏的内容</div>

<!-- CSS display: none：样式上隐藏 -->
<div style="display: none;">隐藏的内容</div>

<!-- CSS visibility: hidden：不可见但占位 -->
<div style="visibility: hidden;">隐藏的内容</div>

<!-- CSS opacity: 0：透明但可交互 -->
<div style="opacity: 0;">隐藏的内容</div>
```

| 方式               | 是否渲染 | 是否占位 | 是否可交�� |
| ------------------ | -------- | -------- | ---------- |
| hidden             | 否       | 否       | 否         |
| display: none      | 否       | 否       | 否         |
| visibility: hidden | 是       | 是       | 否         |
| opacity: 0         | 是       | 是       | 是         |

### 使用场景

```html
<!-- 条件显示 -->
<div id="error-message" hidden>发生错误</div>

<!-- 模态框 -->
<dialog hidden>对话框内容</dialog>

<!-- 折叠内容 -->
<details>
  <summary>点击展开</summary>
  <div>详细内容</div>
</details>
```

## tabindex 属性

`tabindex` 控制元素的 Tab 键焦点顺序：

```html
<input type="text" tabindex="1" />
<input type="text" tabindex="2" />
<button tabindex="3">提交</button>
```

### tabindex 的值

| 值   | 行为                                        |
| ---- | ------------------------------------------- |
| 正数 | 按数值顺序聚焦                              |
| 0    | 按 DOM 顺序聚焦                             |
| -1   | 不能通过 Tab 聚焦，但可以用 JavaScript 聚焦 |

### 常见用法

```html
<!-- 让非交互元素可聚焦 -->
<div tabindex="0" role="button">自定义按钮</div>

<!-- 从 Tab 顺序中移除 -->
<input type="text" tabindex="-1" />

<!-- 程序化聚焦 -->
<div id="modal" tabindex="-1">模态框内容</div>
```

```javascript
document.getElementById('modal').focus()
```

### 最佳实践

1. **避免使用正数 tabindex**：会打乱自然顺序，难以维护
2. **使用 0 或 -1**：0 加入自然顺序，-1 仅程序聚焦
3. **保持逻辑顺序**：调整 DOM 顺序而不是 tabindex

```html
<!-- 不好：用 tabindex 调整顺序 -->
<button tabindex="2">第二</button>
<button tabindex="1">第一</button>
<button tabindex="3">第三</button>

<!-- 好：调整 DOM 顺序 -->
<button>第一</button>
<button>第二</button>
<button>第三</button>
```

## title 属性

`title` 提供元素的额外信息，鼠标悬停时显示：

```html
<abbr title="HyperText Markup Language">HTML</abbr>

<button title="点击保存当前文档">保存</button>
```

### title 的局限性

- 触屏设备无法触发
- 键盘用户无法看到
- 屏幕阅读器支持不一致

因此，重要信息不应该只放在 title 中。

### 替代方案

```html
<!-- 使用可见文本 -->
<button>
  <span class="icon">💾</span>
  <span>保存文档</span>
</button>

<!-- 使用 aria-label -->
<button aria-label="保存文档">
  <span class="icon">💾</span>
</button>

<!-- 使用 CSS 工具提示 -->
<button class="tooltip" data-tip="保存文档">
  <span class="icon">💾</span>
</button>
```

## lang 属性

`lang` 指定元素内容的语言：

```html
<html lang="zh-CN">
  <p>这是中文内容。</p>
  <p lang="en">This is English content.</p>
  <p lang="ja">これは日本語です。</p>
</html>
```

### lang 的用途

1. 屏幕阅读器选择正确的发音
2. 浏览器选择正确的引号样式
3. CSS `:lang()` 伪类
4. 搜索引擎了解页面语言

```css
/* 根据语言应用不同样式 */
:lang(zh) {
  font-family: 'Microsoft YaHei', sans-serif;
}

:lang(en) {
  font-family: 'Segoe UI', sans-serif;
}

:lang(ja) {
  font-family: 'Yu Gothic', sans-serif;
}
```

## dir 属性

`dir` 指定文本方向：

```html
<!-- 从左到右（默认） -->
<p dir="ltr">English text</p>

<!-- 从右到左 -->
<p dir="rtl">النص العربي</p>

<!-- 自动检测 -->
<p dir="auto">内容决定方向</p>
```

主要用于支持阿拉伯语、希伯来语等从右到左书写的语言。

## contenteditable 属性

`contenteditable` 让元素可以编辑：

```html
<div contenteditable="true">这段文字可以编辑</div>
```

### 值

- `true`：可编辑
- `false`：不可编辑
- `inherit`：继承父元素（默认）

### 用途

```html
<!-- 简单的富文本编辑器 -->
<div contenteditable="true" class="editor">在这里输入内容...</div>

<!-- 可编辑标题 -->
<h1 contenteditable="true">点击编辑标题</h1>
```

```javascript
const editor = document.querySelector('.editor')
editor.addEventListener('input', () => {
  console.log(editor.innerHTML)
})
```

## draggable 属性

`draggable` 控制元素是否可拖拽：

```html
<div draggable="true">可以拖拽</div>
<div draggable="false">不能拖拽</div>
```

配合拖放 API 使用：

```javascript
const item = document.querySelector('[draggable="true"]')

item.addEventListener('dragstart', (e) => {
  e.dataTransfer.setData('text/plain', e.target.id)
})

const dropzone = document.querySelector('.dropzone')
dropzone.addEventListener('drop', (e) => {
  const id = e.dataTransfer.getData('text/plain')
  // 处理放置
})
```

## spellcheck 属性

`spellcheck` 控制拼写检查：

```html
<!-- 启用拼写检查 -->
<textarea spellcheck="true"></textarea>

<!-- 禁用拼写检查 -->
<input type="text" spellcheck="false" />
```

代码输入框通常应禁用拼写检查。

## autocapitalize 属性

`autocapitalize` 控制移动设备的自动大写：

```html
<!-- 不自动大写 -->
<input type="text" autocapitalize="none" />

<!-- 首字母大写（默认） -->
<input type="text" autocapitalize="sentences" />

<!-- 每个单词首字母大写 -->
<input type="text" autocapitalize="words" />

<!-- 全部大写 -->
<input type="text" autocapitalize="characters" />
```

## slot 属性

`slot` 用于 Web Components 的插槽分发：

```html
<my-component>
  <span slot="title">标题内容</span>
  <span slot="content">主体内容</span>
</my-component>
```

## 布尔属性

有些属性是布尔属性，只要存在就表示 true：

```html
<!-- 这些都表示 disabled -->
<input disabled />
<input disabled="" />
<input disabled="disabled" />
<input disabled="true" />

<!-- 移除属性表示 false -->
<input />
```

常见的布尔属性：

- `disabled`
- `checked`
- `selected`
- `readonly`
- `required`
- `hidden`
- `autofocus`
- `autoplay`
- `controls`
- `loop`
- `muted`

## 属性的优先级

当 HTML 属性和 CSS/JavaScript 冲突时：

```html
<div style="color: red;" class="blue-text">
  颜色是红色（内联样式优先级更高）
</div>

<input type="text" value="HTML 值" />
```

```javascript
input.value = 'JavaScript 值' // 会覆盖
input.getAttribute('value') // 仍然是 "HTML 值"
```

## 小结

这篇文章介绍了 HTML 的常用全局属性：

- **id**：唯一标识符，用于 CSS、JS、锚点、label 关联
- **class**：类名，主要用于 CSS 样式
- **style**：内联样式，优先级高但不推荐大量使用
- **data-\***：自定义数据属性
- **hidden**：隐藏元素
- **tabindex**：控制 Tab 键焦点顺序
- **title**：悬停提示
- **lang**：语言
- **dir**：文本方向
- **contenteditable**：可编辑
- **draggable**：可拖拽

下一篇开始进入 HTML5 新特性部分。
